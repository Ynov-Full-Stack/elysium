{% extends 'base.html.twig' %}

{% block title %}Elysium | Réservez l’exception{% endblock %}

{% block body %}
    <div class="min-h-screen bg-[#F5F1E8] flex flex-col">
        <!-- Hero Section -->
        <section class="flex flex-col items-center justify-center text-center flex-grow px-6 py-24 bg-gradient-to-b from-[#0C325C]/90 to-[#F5F1E8] text-white relative overflow-hidden">
            {% for message in app.flashes('success') %}
                <div class="mb-6 bg-emerald-50 border border-emerald-200 text-emerald-800 px-4 py-3 rounded-lg text-sm">
                    {{ message }}
                </div>
            {% endfor %}
            <div class="absolute inset-0 bg-[url('{{ asset('images/concert-bg.jpg') }}')] bg-cover bg-center opacity-20"></div>
            <div class="relative z-10 max-w-2xl">
                <h1 class="text-5xl md:text-6xl font-serif tracking-tight mb-6">
                    Réservez l’exception
                </h1>
                <p class="text-lg md:text-xl text-[#E8E3D7] mb-8">
                    Accédez à des expériences musicales et culturelles réservées aux amateurs d’exception.
                </p>
                <div class="flex flex-col sm:flex-row justify-center gap-4">
                    <a href="#"
                       class="bg-[#C59A3A] text-[#F5F1E8] px-8 py-3 rounded-lg font-medium hover:bg-[#D3AD55] transition">
                        Découvrir les spectacles
                    </a>
                    <a href="#"
                       class="border border-white text-white px-8 py-3 rounded-lg font-medium hover:bg-white hover:text-[#0C325C] transition">
                        Devenir membre
                    </a>
                </div>
            </div>
        </section>

        <!-- Section Spectacles -->
        <section class="max-w-7xl mx-auto px-6 py-20">
            <h2 class="text-3xl font-semibold text-[#0C325C] text-center mb-10">Spectacles à l’affiche</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
{#                {% for show in featuredShows %}#}
{#                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden border border-[#E0D4BF] hover:shadow-2xl transition">#}
{#                        <img src="{{ asset(show.image) }}" alt="{{ show.title }}" class="w-full h-56 object-cover">#}
{#                        <div class="p-6">#}
{#                            <h3 class="text-xl font-semibold text-[#0C325C] mb-2">{{ show.title }}</h3>#}
{#                            <p class="text-[#7A6B56] text-sm mb-4">{{ show.date|date('d F Y') }} • {{ show.location }}</p>#}
{#                            <a href="{{ path('app_show_detail', {id: show.id}) }}"#}
{#                               class="text-[#C59A3A] font-medium hover:underline">#}
{#                                En savoir plus →#}
{#                            </a>#}
{#                        </div>#}
{#                    </div>#}
{#                {% else %}#}
{#                    <p class="text-center text-[#7A6B56] col-span-3">#}
{#                        Aucun spectacle disponible pour le moment.#}
{#                    </p>#}
{#                {% endfor %}#}
            </div>
        </section>
    </div>
{% endblock %}
